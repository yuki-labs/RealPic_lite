{
  "name": "realpic-lite",
  "version": "1.0.0",
  "description": "Camera app with visible and invisible watermarks",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "dev": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
